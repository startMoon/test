<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.morning.star.dao.ProductDAO">

    <select id="getSpuCode" resultType="String">
      use `tohome`;
      SELECT spuId FROM th_product WHERE
      productCode = (SELECT productCode from th_item_product WHERE itemCode = #{goodsCode})
    </select>

</mapper>